Basic cases for session
=======================

Try to explore sessions from requests lib.

Import session function:: 

  >>> from requests import session
  >>> session
  <function session at ...>

Now the Session class::

  >>> from requests import Session
  >>> session = Session()
  >>> session
  <requests.sessions.Session object at ...>

